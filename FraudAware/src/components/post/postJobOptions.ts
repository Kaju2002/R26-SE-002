import type { JobMode, JobType } from '../../../data/jobs';

export const JOB_TYPE_OPTIONS: JobType[] = [
  'Full-Time',
  'Part-Time',
  'Contract',
  'Internship',
];

export const JOB_MODE_OPTIONS: JobMode[] = ['On-Site', 'Remote', 'Hybrid'];
